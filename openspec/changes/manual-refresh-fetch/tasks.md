## 1. 前端刷新入口与状态编排

- [x] 1.1 在选择状态 store 中实现本地刷新与 Fetch 后的仓库状态、提交、冲突状态和选中 diff 刷新，并处理失效的提交选择
- [x] 1.2 在提交工具栏新增“刷新”和“获取远程”入口，显示对应进度、禁用重复触发并呈现 Fetch 结果

## 2. 后端手动 Fetch

- [x] 2.1 增加带 30 秒超时和进程终止能力的 Git Fetch 执行方法及独立 Tauri `git_fetch(path)` 命令
- [x] 2.2 注册 Tauri 命令，并确认 Fetch 只运行 `git fetch`、不恢复自动 Fetch

## 3. 变更核验

- [x] 3.1 对照验收场景检查本地刷新无网络、Fetch 只更新远程引用、错误/超时后刷新且进度可恢复
- [x] 3.2 检查代码差异和格式错误；不运行 Vue 类型检查、测试或打包命令
