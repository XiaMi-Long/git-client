## 1. 历史视图状态与切换入口

- [x] 1.1 将二态历史设置升级为六种视图，兼容旧 `commitListMode`，增加带用途说明且支持键盘导航的视图选择器
- [x] 1.2 统一视图提交选择与分支范围，切换视图保留各自滚动、筛选和提交选择，切仓库时清理仓库特定状态和过期响应
- [x] 1.3 在右侧提交文件列表增加进入该文件历史的快捷操作

## 2. Git 历史查询能力

- [x] 2.1 扩展 `LogQuery` 支持 pathspec、拓扑排序和日期区间，并确保搜索、分支范围和分页组合生效
- [x] 2.2 增加限定最近 180 天、按 Git 提交时间聚合的活动查询命令并注册到 Tauri
- [x] 2.3 修正标签目标提交解析并为轻量标签提供目标提交时间
- [x] 2.4 增加根据绝对路径定位 Git 仓库根目录的命令

## 3. 提交列表与分支拓扑

- [x] 3.1 在经典列表中增加按日期分组，维持虚拟滚动、分页、搜索和提交选择
- [x] 3.2 新增按拓扑顺序分页的分支图视图，按真实父提交关系连线并处理未加载父节点
- [x] 3.3 保持现有作者泳道可用，并让它与新增视图使用一致的提交选择和范围筛选
- [x] 3.4 默认开启经典列表的日期分组，同时保留手动开关

## 4. 文件、版本与活动视图

- [x] 4.1 新增文件/目录历史视图，定位绝对路径所属 Git 仓库并转换为 Git 相对路径，支持跨仓库详情、右侧文件快捷入口和分页
- [x] 4.2 新增版本标签时间线，支持浏览目标标签及基准到目标之间的提交
- [x] 4.3 新增 180 天活动热力图，支持铺满可用宽度并保持适中格子、选日和提交明细
- [x] 4.4 为新视图补齐加载、空结果、错误、仓库切换及异步过期响应状态
- [x] 4.5 统一分支拓扑、提交活动的文字层级，应用主题样式到文件历史按钮和设置视图选择控件

## 5. 变更核验

- [x] 5.1 对照视图切换、状态保留、路径/标签/日期范围和右侧 diff 场景静态检查代码
- [x] 5.2 验证 OpenSpec 变更有效并检查差异；不运行 Vue 类型检查、测试或打包命令
- [x] 5.3 静态核对外部仓库文件历史的提交选择、只读上下文和右侧 diff 路径
