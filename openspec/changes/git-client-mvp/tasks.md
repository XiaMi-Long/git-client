## 1. 项目初始化与基础设施

- [x] 1.1 用 Tauri 2 + Vue 3 脚手架初始化项目，验证：`cargo tauri dev` 能起空窗口
- [x] 1.2 搭建目录结构（`src-tauri/src/{git,watcher,commands}`、`src/{components,views,stores,composables,styles}`），验证：目录存在且 import 路径可用
- [x] 1.3 落地设计语言 token（CSS 变量，暗色 `:root` + 亮色 `[data-theme="light"]`），验证：组件引用变量取色正确
- [x] 1.4 实现主题切换与持久化，验证：切换亮暗即时生效且重启保持
- [x] 1.5 全局基础组件中文化，验证：按钮 / 菜单 / 占位符均为简体中文

## 2. Rust 后端 git 封装

- [x] 2.1 git 版本检测命令，验证：启动时打印 git 版本，低于阈值告警
- [x] 2.2 `git status --porcelain` 解析为工作区文件状态结构，验证：增删改状态正确分类
- [x] 2.3 `git log --format` 分页查询（`--skip` / `-n`），验证：能按页取 100 条
- [x] 2.4 `git diff` 解析（统一格式 + 按文件），验证：hunk / 增删行解析正确
- [x] 2.5 `git rev-parse` / branch / tag 查询封装，验证：分支与标签列表正确
- [x] 2.6 分支操作命令封装（新建 / 检出 / 删除 / 重命名 / merge），验证：各命令行为与 CLI 一致
- [x] 2.7 cherry-pick 命令封装，验证：能应用指定提交
- [x] 2.8 pull / push 命令封装，验证：拉取推送成功并返回结果
- [x] 2.9 冲突状态检测（解析 `git status` 冲突标记），验证：冲突文件被正确识别

## 3. 文件监听

- [x] 3.1 notify crate 监听工作区，验证：文件改动触发事件（代码完成 + cargo check 通过；运行时联调待 5 组打开仓库）
- [x] 3.2 .gitignore 过滤，验证：被忽略文件不触发（代码完成 + cargo check 通过；运行时联调待 5 组）
- [x] 3.3 500ms 防抖 + IPC 通知前端，验证：批量改动只刷新一次（代码完成 + cargo check 通过；运行时联调待 5 组）

## 4. 前端三栏布局

- [x] 4.1 顶栏（仓库标签页 + 分支选择 + 拉取 / 推送 + 搜索框），验证：布局与 UI 规范尺寸一致
- [x] 4.2 侧栏（分支 / 远程 / 标签树），验证：树缩进与节点样式符合规范
- [x] 4.3 提交列表区（含 mini 图谱列占位），验证：列宽与列表项高度符合规范
- [x] 4.4 右侧上下分栏 + 可拖拽分隔条，验证：拖拽改变文件列表 / diff 区比例
- [x] 4.5 状态栏，验证：连接状态 / 领先落后 / 编码展示正确

## 5. 仓库管理

- [x] 5.1 打开仓库（目录选择 + .git 校验），验证：有效仓库创建标签，无效目录提示（代码完成 + 编译通过；运行时待联调）
- [x] 5.2 多仓库标签页 + 独立 store 分片，验证：各标签状态互不串扰（代码完成 + 编译通过；运行时待联调）
- [x] 5.3 标签页切换 / 关闭 + 状态保留，验证：切回标签恢复滚动位置与选中项（代码完成 + 编译通过；运行时待联调）
- [x] 5.4 连接状态展示，验证：有 / 无远程显示正确（代码完成 + 编译通过；运行时待联调）

## 6. 提交历史

- [x] 6.1 提交列表渲染（哈希 / 信息 / 作者 / 时间），验证：字段与时间倒序正确（代码完成 + 编译通过；运行时待联调）
- [x] 6.2 分页加载（滚动到底加载下一页），验证：追加且不重复（代码完成 + 编译通过；运行时待联调）
- [x] 6.3 虚拟滚动，验证：大列表仅渲染可视区（代码完成 + 编译通过；运行时待联调）
- [x] 6.4 mini 图谱渲染 + 分支着色，验证：节点连线与 HEAD 主色环正确（代码完成 + 编译通过；运行时待联调）
- [x] 6.5 当前分支 / 所有分支切换，验证：列表范围切换正确（已修复后端 --all；运行时待联调）
- [x] 6.6 侧栏单击浏览分支历史，验证：列表切换且工作区不切换（代码完成 + 编译通过；运行时待联调）
- [x] 6.7 搜索（信息 / 作者 / 哈希，300ms 防抖），验证：过滤结果正确（代码完成 + 编译通过；运行时待联调）

## 7. 工作区

- [x] 7.1 工作区伪节点（钉顶 + 未提交数），验证：显示"工作区 (N)"且 N 正确（代码完成 + 编译通过；运行时待联调）
- [x] 7.2 工作区模式切换（右侧三段 + 提交框），验证：点击进入、选其他提交退出（代码完成 + 编译通过；运行时待联调）
- [x] 7.3 已暂存 / 未暂存两组，验证：文件分组正确（代码完成 + 编译通过；运行时待联调）
- [x] 7.4 文件级暂存 / 取消暂存，验证：文件在两组间移动（代码完成 + 编译通过；运行时待联调）
- [ ] 7.5 hunk 级暂存，验证：仅暂存所选 hunk（需后端加 git apply --cached hunk 命令）
- [x] 7.6 提交（信息输入 + Ctrl+Enter + 刷新），验证：提交后工作区与列表刷新、空信息拦截（代码完成 + 编译通过；运行时待联调）

## 8. diff 视图

- [x] 8.1 统一视图渲染（行号 / 增删色 / hunk 头），验证：配色与行号正确（代码完成 + 编译通过；运行时待联调）
- [x] 8.2 词级高亮，验证：行内改动词片段高亮（代码完成 + 编译通过；运行时待联调）
- [x] 8.3 双栏视图切换，验证：左右对比正确（代码完成 + 编译通过；运行时待联调）
- [x] 8.4 按文件懒加载，验证：仅选中文件加载 diff（代码完成 + 编译通过；运行时待联调）
- [x] 8.5 自动换行开关，验证：开关切换行宽行为（代码完成 + 编译通过；运行时待联调）

## 9. 分支操作

- [x] 9.1 新建分支（含立即检出选项），验证：创建并可立即检出（代码完成 + 编译通过；运行时待联调）
- [x] 9.2 检出（双击侧栏 + 未提交冲突拦截），验证：检出成功且有冲突时拦截（代码完成 + 编译通过；运行时待联调）
- [x] 9.3 删除分支（确认 + 当前分支拦截），验证：确认后删除、当前分支拒绝（代码完成 + 编译通过；运行时待联调）
- [x] 9.4 重命名分支，验证：重命名后侧栏刷新（代码完成 + 编译通过；运行时待联调）
- [x] 9.5 合并到当前，验证：合并成功后列表与状态刷新（代码完成 + 编译通过；运行时待联调）
- [x] 9.6 领先 / 落后对比，验证：对比展示领先落后数（后端 git_compare_branches + 前端精确对比；运行时待联调）

## 10. 远程同步

- [x] 10.1 拉取（Ctrl+P），验证：拉取成功后列表与状态刷新、无远程提示（代码完成 + 编译通过；运行时待联调）
- [x] 10.2 推送（Ctrl+Shift+P），验证：推送成功后领先落后刷新（代码完成 + 编译通过；运行时待联调）
- [x] 10.3 凭据助手鉴权，验证：首次走系统凭据不报错（后端走 git credential helper；状态栏"正在鉴权"提示待加载提示系统）
- [x] 10.4 领先 / 落后状态展示，验证：状态栏显示"领先 X 落后 Y"（第 5 组 StatusBar 已实现）

## 11. cherry-pick

- [x] 11.1 提交右键 cherry-pick，验证：成功后工作区与列表刷新（代码完成 + 编译通过；运行时待联调）
- [x] 11.2 cherry-pick 冲突转入冲突处理，验证：冲突时标记冲突文件（代码完成 + 编译通过；运行时待联调）

## 12. 冲突处理

- [x] 12.1 冲突检测，验证：操作后冲突被识别并进入冲突状态（代码完成 + 编译通过；运行时待联调）
- [x] 12.2 冲突文件列表 + ⚠ 标记，验证：冲突文件正确展示（代码完成 + 编译通过；运行时待联调）
- [x] 12.3 标记已解决，验证：文件移出冲突列表（代码完成 + 编译通过；运行时待联调）
- [x] 12.4 继续 / 中止（状态栏），验证：继续完成操作、中止恢复操作前状态（代码完成 + 编译通过；运行时待联调）

## 13. 验证

- [ ] 13.1 用真实仓库跑通拉取 / 推送 / 提交 / 分支 / cherry-pick 全流程，验证：无报错且状态正确
- [ ] 13.2 大仓库性能验证（分页 / 虚拟滚动 / 懒加载 diff），验证：滚动与切换流畅不卡顿
- [x] 13.3 主题切换与中文文案检查，验证：亮暗切换正确、无英文残留（代码检查通过：theme.ts 持久化 + tokens 亮暗 + UI 文案全中文）
- [x] 13.4 `openspec validate "git-client-mvp"` 通过，验证：规范合法
